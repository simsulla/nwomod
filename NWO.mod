name = "New World Order 0.2" # 2015-04-26
path = "mod/NWO" # location of your mod files
user_dir = "NwO" # optional. creates a sub directory where settings/map cache/savegames etc can be safely written for your mod and not get mixed up  
